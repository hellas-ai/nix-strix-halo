# Drop-in patches for amd-aiter applied on top of upstream packaging.
# Each *.patch file in this directory is appended to the derivation's
# patches list. Currently empty — see TODO in pkgs/amd-aiter/default.nix.
