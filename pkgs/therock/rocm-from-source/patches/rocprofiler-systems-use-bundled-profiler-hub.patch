--- a/rocm-systems/projects/rocprofiler-systems/cmake/ProfilerHub.cmake
+++ b/rocm-systems/projects/rocprofiler-systems/cmake/ProfilerHub.cmake
@@ -50,6 +50,14 @@ if(profiler-hub_FOUND)
     message(STATUS "[profiler-hub] Using installed package: ${profiler-hub_DIR}")
     set(_PROFILER_HUB_IS_EXTERNAL TRUE)
 else()
+    set(
+        _PROFILER_HUB_BUNDLED_SOURCE_DIR
+        "${CMAKE_CURRENT_LIST_DIR}/../../../profilers/profiler-hub"
+    )
+    if(EXISTS "${_PROFILER_HUB_BUNDLED_SOURCE_DIR}/CMakeLists.txt")
+        message(STATUS "[profiler-hub] Using bundled source: ${_PROFILER_HUB_BUNDLED_SOURCE_DIR}")
+        set(_PROFILER_HUB_SOURCE_DIR "${_PROFILER_HUB_BUNDLED_SOURCE_DIR}")
+    else()
     message(
         STATUS
         "[profiler-hub] find_package failed; falling back to "
@@ -185,6 +193,7 @@ else()
             "[profiler-hub] Reusing existing sparse checkout at ${_PROFILER_HUB_SOURCE_DIR}"
         )
     endif()
+    endif()
 endif()

 if(NOT _PROFILER_HUB_IS_EXTERNAL)
